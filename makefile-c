##############################################################################
################################ makefile-c ##################################
##############################################################################
#                                                                            #
#   complete makefile of SingleFlowDCRBlock                                  #
#                                                                            #
#   The makefile defines internally all external libraries (comprised the    #
#   SMS++ one) required by SingleFlowDCRBlock. Accordingly, all necessary    #
#   OBJ, H, -I directives, external libraries + -L< libdirs > are added to   #
#   the corresponding MCFBk* macros.                                         #
#                                                                            #
#   Input:  $(CC)       = compiler command                                   #
#           $(SW)       = compiler options                                   #
#           $(SFDCRSDR) = the directory where the SingleFlowDCRBlock         #
#                         source is                                          #
#                                                                            #
#   Output: $(SFDCROBJ) = the final object(s) / library                      #
#           $(SFDCRLIB) = external libraries + -L< libdirs >                 #
#           $(SFDCRH)   = the .h files to include for SingleFlowDCRBlock     #
#           $(SFDCRINC) = the -I$( SingleFlowDCRBlock directory   )          #
#                                                                            #
#                              Antonio Frangioni                             #
#                         Dipartimento di Informatica                        #
#                             Universita' di Pisa                            #
#                                                                            #
##############################################################################

# define & include the necessary modules- - - - - - - - - - - - - - - - - - -
# if a module is not used in the current configuration, just comment out the
# corresponding include line
# each module outputs some macros to be used here:
# *OBJ is the final object/library
# *H   is the list of all include files
# *INC is the -I< include directories >

# define input macros for SMS++ complete makefile, then include it
SMS++SDR = $(SFDCRSDR)/../SMS++
include $(SMS++SDR)/lib/makefile-inc

MILPINC = $(SFDCRSDR)/../MILPSolver/include

# include the makefile requiring all external modules in input
include $(SFDCRSDR)/makefile

# macros to be exported - - - - - - - - - - - - - - - - - - - - - - - - - - -

# append external -I directives to SFDCRINC, SFDCROBJ and SFDCRH as defined
# by the makefile, thus the := assignment has to be used (use GNU make)
SFDCROBJ := $(SFDCROBJ) $(SMS++OBJ) $(libMCFClOBJ)
SFDCRINC := $(SFDCRINC) $(SMS++INC) $(libMCFClINC) -I$(MILPINC)

# external libraries for SingleFlowDCRBlock
SFDCRLIB = $(SMS++LIB) $(libMCFClLIB) -I$(MILPINC)

############################ End of makefile #################################
